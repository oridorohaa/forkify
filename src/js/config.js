// variables that are resposible for defining
// some important data about the application itself

//1. ---- example: api url
// if the url needs to change, dont needto change it
//everywhere  -- just change it in the configuration file
export const API_URL = ` https://forkify-api.herokuapp.com/api/v2/recipes/`;
// use upper cases for variables that never change
export const TIMEOUT_SEC = 10;
export const RES_PER_PAGE = 5;
